import React from 'react';
import { shallow } from 'enzyme';
import { expect } from 'chai';
import sinon from 'sinon';
import Flag from './';

describe('<Flag />', () => {
  it('Should display modal when delete is clicked', () => {
    
  });
});
